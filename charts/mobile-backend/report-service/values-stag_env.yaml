
# environment: stag-env

# global:
#   namespace: one-meristem-app
#   serviceAccountName: app-spc-sa
#   create: false     # Prevent chart from trying to create it

# networkpolicy:
#   enabled: false #network policy


# replicaCount: 2

# image:
#   repository: swr.af-south-1.myhuaweicloud.com/swr-repo-one-meristem-app-stag-env/report-service
#   pullPolicy: Always
#   tag: 05e34d9-86

# imagePullSecrets:
#   - name: huawei-swr-auth #relying on CCE's automatic injection

# # Secret provider class
# secretProviderClass:
#   name: report-service-secrets
#   provider: cce
#   k8sSecretName: report-service-secrets


# strategy:
#   type: RollingUpdate
#   rollingUpdate:
#     maxUnavailable: 0
#     maxSurge: 1

# resources:
#   limits:
#     cpu: "1"
#     memory: 800Mi
#   requests:
#     cpu: "0.5"
#     memory: 512Mi

# volumeMounts:
#   - name: report-service-secrets
#     mountPath: /mnt/secrets-store/report-service
#     readOnly: true
#   - name: report-data
#     mountPath: /mnt/report
#   - name: secret-volume
#     mountPath: /etc/secrets-store/report-service
#     readOnly: true

# volumes:
#   - name: report-data
#     emptyDir: {}
#   - name: report-service-secrets
#     csi:
#       driver: secrets-store.csi.k8s.io
#       readOnly: true
#       volumeAttributes:
#         secretProviderClass: report-service-secrets
#         #csi.storage.k8s.io/provisioner-secret-name: report-service-secrets
#         #csi.storage.k8s.io/provisioner-secret-namespace: one-meristem-app
#   - name: secret-volume
#     secret:
#       secretName: credentials


# env:
#   COMPANY_NAME: "Meristem Securities Limited"
#   DATA_PRIVACY_POLICY_URL: "https://www.onemerristem.merristem.com"
#   GREEN_LOGO_URL: "https://obs-unstructured-logo-one-meristem-stag_env.obs.af-south-1.myhuaweicloud.com/Meristem_Green_Logo.png"
#   WHITE_LOGO_URL: "https://obs-unstructured-logo-one-meristem-stag_env.obs.af-south-1.myhuaweicloud.com/Meristem_White_Logo.png"
#   FINANCE_LOGO_URL: "https://obs-unstructured-logo-one-meristem-stag_env.obs.af-south-1.myhuaweicloud.com/Meristem_Finance_Logo.png"
#   GITHUB_BRANCH: "test_env"
#   APP_PROFILE: "staging"
#   # SERVER_PORT: "20050"
#   ONE_APP_SERVER_URL: "https://onemeristemstag.meristemng.com" #placeholder for the EIP IP
#   ONE_APP_EMAIL: "hello@onemeristem.meristemng.com"
#   ADMIN_USERNAME: "admin"
#   CONFIG_SERVER_URI: "http://config-server-stag-env.one-meristem-app.svc.cluster.local:8082/config"
#   CREDIT_SWITCH_LOGIN_ID: "315497"
#   CREDIT_SWITCH_SMS_MERCHANT_NAME: "Meristem Securities Limited"
#   CREDIT_SWITCH_SMS_BASEURL: "http://176.58.99.160:9014/api/v1/sms/send"
#   CREDIT_SWITCH_SMS_SENDER_ID: "Meristem"
#   REPORT_SERVICE_HOSTNAME: "report-service-stag-env.one-meristem-app.svc.cluster.local"
#   REPORT_SERVICE_PORT: "8082"
#   REPORT_SERVICE_URI: "http://report-service-stag-env.one-meristem-app.svc.cluster.local:8082"
#   CONSUL_HOST: "consul-stag-env.one-meristem-app.svc.cluster.local"
#   USER_SERVICE_URI: "http://users-service-stag-env.one-meristem-app.svc.cluster.local:8082"
#   TRUSTEES_CLIENT_ID: "trustees-service"
#   HUAWEI_OBS_UNSTRUCTURED_BUCKET: "obs-unstructured-logo-one-meristem-stag-env"
#   MERISTEM_GREEN_LOGO: "https://obs-unstructured-logo-one-meristem-stag-env.obs.af-south-1.myhuaweicloud.com/Meristem_Green_Logo.png"
#   SUPPORT_PHONE: "0700-MERISTEM"
#   SUPPORT_EMAIL: "support@onemeristem.com"
#   ENC_ALGORITHM: "AES"
#   ENC_ALGORITHM_GCM: "AES/GCM/NoPadding"



# secretEnv:
#   ADMIN_PASSWORD:
#     secretName: credentials
#     secretKey: key3
#   ENCRYPTION_KEY:
#     secretName: credentials
#     secretKey: key4
#   DB_SERVER:
#     secretName: report-service-secrets
#     secretKey: DB_SERVER
#   REPORT_DB_NAME:
#     secretName: report-service-secrets
#     secretKey: REPORT_DB_NAME
#   REPORT_DB_USERNAME:
#     secretName: report-service-secrets
#     secretKey: REPORT_DB_USERNAME
#   REPORT_DB_PASSWORD:
#     secretName: report-service-secrets
#     secretKey: REPORT_DB_PASSWORD
#   KAFKA_HOST_URL:
#     secretName: report-service-secrets
#     secretKey: KAFKA_HOST_URL
#   REDIS_HOST:
#     secretName: report-service-secrets
#     secretKey: REDIS_HOST
#   REDIS_PASSWORD:
#     secretName: report-service-secrets
#     secretKey: REDIS_PASSWORD
#   MAILER_HOST:
#     secretName: credentials
#     secretKey: key5
#   MAILER_USERNAME:
#     secretName: credentials
#     secretKey: key6
#   MAILER_PASSWORD:
#     secretName: credentials
#     secretKey: key7
#   CREDIT_SWITCH_PUBLIC_KEY:
#     secretName: credentials
#     secretKey: key8
#   CREDIT_SWITCH_PRIVATE_KEY:
#     secretName: credentials
#     secretKey: key9
#   CREDIT_SWITCH_SMS_PASSWORD:
#     secretName: credentials
#     secretKey: key10
#   H2_PASSWORD:
#     secretName: credentials
#     secretKey: key13
#   TRUSTEES_CLIENT_SECRET: 
#     secretName: credentials
#     secretKey: key16
#   ENC_SALT:
#     secretName: credentials
#     secretKey: key17

# #PVC configuration
# # pvc:
# #   name: report-service-pvc-evs
# #   accessModes:
# #     - ReadWriteOnce
# #   resources:
# #     requests:
# #       storage: 20Gi
# #   storageClassName: csi-disk-topology
# #   volumeMode: Filesystem

# # =============================================================================
# # SCHEDULING & AVAILABILITY CONFIGURATION
# # =============================================================================
# affinity:
#   # Keep pods in the correct Node Pool
#   nodeAffinity:
#     requiredDuringSchedulingIgnoredDuringExecution:
#       nodeSelectorTerms:
#         - matchExpressions:
#             - key: nodepool
#               operator: In
#               values:
#                 - one-meristem-backend

#   # Ensure the 2 pod replicas never share the same physical node
#   # ALLOW HPA to scale 
#   podAntiAffinity:
#     preferredDuringSchedulingIgnoredDuringExecution:
#     - weight: 100
#       podAffinityTerm:
#         labelSelector:
#           matchExpressions:
#             - key: app
#               operator: In
#               values:
#                 - report-service-stag-env
#         topologyKey: "kubernetes.io/hostname"  

# # HorizontalPodAutoscaler configuration
# autoscaling:
#   enabled: false
#   minReplicas: 2
#   maxReplicas: 10
#   targetCPUUtilizationPercentage: 70
#   targetMemoryUtilizationPercentage: 70

# #Prometheus Config
# prometheus:
#   enabled: true
#   path: /api/reports/actuator/prometheus
#   port: 20050

# # service configuration
# service:
#   type: ClusterIP
#   name: report-service
#   port: 8082
#   targetPort: 20050
#   annotationsEnabled: false
#   annotations:
#     #------- Prometheus registration -----------
#     prometheus.io/scrape: "true"
#     prometheus.io/port: "20050"
#     prometheus.io/path: "/api/reports/actuator/prometheus"
#     # ---- Consul service registration ----
#     consul.hashicorp.com/service-name: "report-service"
#     #consul.hashicorp.com/service-port: "8082"
#     consul.hashicorp.com/service-tags: "springboot,backend,report"
#     consul.hashicorp.com/service-meta-environment: "stag-env"

#     # Optional: Define Consul health check
#     consul.hashicorp.com/check-path: "/api/reports/actuator/health"
#     consul.hashicorp.com/check-interval: "10s"
#     consul.hashicorp.com/check-timeout: "5s"

# # To overiede the default name and fullname of the chart (for networkpolicy)
# nameOverride: ""
# fullnameOverride: ""



##--------------------- REPORT-SERVICE STAGING VALUES ---------------------
## Spring Boot Report Service — reports, transaction histories, financial records
## Node pool: backend | Namespace: one-meristem-app
## Port: 20050 (actuator/app) | Service port: 8082

environment: stag-env

global:
  namespace: one-meristem-app
  serviceAccountName: app-spc-sa
  create: false

# Reserved for future middleware network policy
networkpolicy:
  enabled: false

# For network policy name/fullname override
nameOverride: ""
fullnameOverride: ""

# =============================================================================
# DEPLOYMENT
# =============================================================================
replicaCount: 2

image:
  repository: swr.af-south-1.myhuaweicloud.com/swr-repo-one-meristem-app-stag-env/report-service
  pullPolicy: Always
  tag: ""

imagePullSecrets:
  - name: huawei-swr-auth

terminationGracePeriodSeconds: 60

strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 0
    maxSurge: 1

# =============================================================================
# RESOURCES
# =============================================================================
resources:
  requests:
    cpu: "500m"
    memory: "512Mi"
  limits:
    cpu: "1000m"
    memory: "800Mi"

# =============================================================================
# HEALTH PROBES
# =============================================================================
probes:
  startup:
    path: /api/reports/actuator/health
    failureThreshold: 30
    periodSeconds: 10

  liveness:
    path: /api/reports/actuator/health
    initialDelaySeconds: 120
    periodSeconds: 40
    timeoutSeconds: 5
    failureThreshold: 5

  readiness:
    path: /api/reports/actuator/health
    initialDelaySeconds: 60
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 5

# =============================================================================
# SCHEDULING & AVAILABILITY
# =============================================================================
nodePool: one-meristem-backend

affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
        - matchExpressions:
            - key: node_pool
              operator: In
              values:
                - one-meristem-backend
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
              - key: app
                operator: In
                values:
                  - report-service-stag-env
          topologyKey: kubernetes.io/hostname

# =============================================================================
# AUTOSCALING — CCE CustomedHPA
# Report generation is CPU intensive — higher ceiling
# =============================================================================
hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 6
  minScaleStep: 1
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      percentValue: 100
      podsValue: 2
    scaleDown:
      stabilizationWindowSeconds: 300
      percentValue: 25

# =============================================================================
# SECRET PROVIDER CLASS
# =============================================================================
secretProviderClass:
  name: report-service-secrets
  provider: cce
  k8sSecretName: report-service-secrets

# =============================================================================
# NETWORK POLICY — egress to middleware namespace
# Enable when middleware services are deployed
# =============================================================================
networkpolicy:
  enabled: true    
  middlewareNamespace: middleware-core-app
  middlewarePorts:  
    - 8080    # all middleware API services listen here

nameOverride: ""
fullnameOverride: ""

# =============================================================================
# ENVIRONMENT VARIABLES
# =============================================================================
env:
  COMPANY_NAME: "Meristem Securities Limited"
  DATA_PRIVACY_POLICY_URL: "https://www.onemeristem.meristemng.com"
  GREEN_LOGO_URL: "https://obs-unstructured-logo-one-meristem-stag-env.obs.af-south-1.myhuaweicloud.com/Meristem_Green_Logo.png"
  WHITE_LOGO_URL: "https://obs-unstructured-logo-one-meristem-stag-env.obs.af-south-1.myhuaweicloud.com/Meristem_White_Logo.png"
  FINANCE_LOGO_URL: "https://obs-unstructured-logo-one-meristem-stag-env.obs.af-south-1.myhuaweicloud.com/Meristem_Finance_Logo.png"
  GITHUB_BRANCH: "test_env"
  APP_PROFILE: "staging"
  ONE_APP_SERVER_URL: "https://onemeristemstag.meristemng.com"
  ONE_APP_EMAIL: "hello@onemeristem.meristemng.com"
  ADMIN_USERNAME: "admin"
  CONFIG_SERVER_URI: "http://config-server-stag-env.one-meristem-app.svc.cluster.local:8082/config"
  CREDIT_SWITCH_LOGIN_ID: "315497"
  CREDIT_SWITCH_SMS_MERCHANT_NAME: "Meristem Securities Limited"
  CREDIT_SWITCH_SMS_BASEURL: "http://176.58.99.160:9014/api/v1/sms/send"
  CREDIT_SWITCH_SMS_SENDER_ID: "Meristem"
  REPORT_SERVICE_HOSTNAME: "report-service-stag-env.one-meristem-app.svc.cluster.local"
  REPORT_SERVICE_PORT: "8082"
  REPORT_SERVICE_URI: "http://report-service-stag-env.one-meristem-app.svc.cluster.local:8082"
  CONSUL_HOST: "consul-stag-env.one-meristem-app.svc.cluster.local"
  USER_SERVICE_URI: "http://users-service-stag-env.one-meristem-app.svc.cluster.local:8082"
  TRUSTEES_CLIENT_ID: "trustees-service"
  HUAWEI_OBS_UNSTRUCTURED_BUCKET: "obs-unstructured-logo-one-meristem-stag-env"
  MERISTEM_GREEN_LOGO: "https://obs-unstructured-logo-one-meristem-stag-env.obs.af-south-1.myhuaweicloud.com/Meristem_Green_Logo.png"
  SUPPORT_PHONE: "0700-MERISTEM"
  SUPPORT_EMAIL: "support@onemeristem.com"
  ENC_ALGORITHM: "AES"
  ENC_ALGORITHM_GCM: "AES/GCM/NoPadding"
  MIDDLE_WARE_BASE_URL: "http://gateway-api-stag-env.middleware-core-app.svc.cluster.local:8080"

# =============================================================================
# SECRET ENV — keys match credentials-secret.yaml template exactly
# =============================================================================
secretEnv:
  # From credentials Kubernetes secret
  ADMIN_PASSWORD:
    secretName: credentials
    secretKey: ADMIN_PASSWORD
  ENCRYPTION_KEY:
    secretName: credentials
    secretKey: ENCRYPTION_KEY
  MAILER_HOST:
    secretName: credentials
    secretKey: MAILER_HOST
  MAILER_USERNAME:
    secretName: credentials
    secretKey: MAILER_USERNAME
  MAILER_PASSWORD:
    secretName: credentials
    secretKey: MAILER_PASSWORD
  CREDIT_SWITCH_PUBLIC_KEY:
    secretName: credentials
    secretKey: CREDIT_SWITCH_PUBLIC_KEY
  CREDIT_SWITCH_PRIVATE_KEY:
    secretName: credentials
    secretKey: CREDIT_SWITCH_PRIVATE_KEY
  CREDIT_SWITCH_SMS_PASSWORD:
    secretName: credentials
    secretKey: CREDIT_SWITCH_SMS_PASSWORD
  H2_PASSWORD:
    secretName: credentials
    secretKey: H2_PASSWORD
  TRUSTEES_CLIENT_SECRET:
    secretName: credentials
    secretKey: TRUSTEES_CLIENT_SECRET
  ENC_SALT:
    secretName: credentials
    secretKey: ENC_SALT
  ID_HASHING_SECRET:
    secretName: credentials
    secretKey: ID_HASHING_SECRET
  WALLET_SECRET:
    secretName: credentials
    secretKey: WALLET_SECRET
  MIDDLE_WARE_API_KEY:
    secretName: credentials
    secretKey: MIDDLE_WARE_API_KEY
    
  # From CSMS via SecretProviderClass
  DB_SERVER:
    secretName: report-service-secrets
    secretKey: DB_SERVER
  REPORT_DB_NAME:
    secretName: report-service-secrets
    secretKey: REPORT_DB_NAME
  REPORT_DB_USERNAME:
    secretName: report-service-secrets
    secretKey: REPORT_DB_USERNAME
  REPORT_DB_PASSWORD:
    secretName: report-service-secrets
    secretKey: REPORT_DB_PASSWORD
  KAFKA_HOST_URL:
    secretName: report-service-secrets
    secretKey: KAFKA_HOST_URL
  REDIS_HOST:
    secretName: report-service-secrets
    secretKey: REDIS_HOST
  REDIS_PASSWORD:
    secretName: report-service-secrets
    secretKey: REDIS_PASSWORD

# secretEnv:
#   ADMIN_PASSWORD:
#     secretName: credentials
#     secretKey: key3
#   ENCRYPTION_KEY:
#     secretName: credentials
#     secretKey: key4
#   DB_SERVER:
#     secretName: report-service-secrets
#     secretKey: DB_SERVER
#   REPORT_DB_NAME:
#     secretName: report-service-secrets
#     secretKey: REPORT_DB_NAME
#   REPORT_DB_USERNAME:
#     secretName: report-service-secrets
#     secretKey: REPORT_DB_USERNAME
#   REPORT_DB_PASSWORD:
#     secretName: report-service-secrets
#     secretKey: REPORT_DB_PASSWORD
#   KAFKA_HOST_URL:
#     secretName: report-service-secrets
#     secretKey: KAFKA_HOST_URL
#   REDIS_HOST:
#     secretName: report-service-secrets
#     secretKey: REDIS_HOST
#   REDIS_PASSWORD:
#     secretName: report-service-secrets
#     secretKey: REDIS_PASSWORD
#   MAILER_HOST:
#     secretName: credentials
#     secretKey: key5
#   MAILER_USERNAME:
#     secretName: credentials
#     secretKey: key6
#   MAILER_PASSWORD:
#     secretName: credentials
#     secretKey: key7
#   CREDIT_SWITCH_PUBLIC_KEY:
#     secretName: credentials
#     secretKey: key8
#   CREDIT_SWITCH_PRIVATE_KEY:
#     secretName: credentials
#     secretKey: key9
#   CREDIT_SWITCH_SMS_PASSWORD:
#     secretName: credentials
#     secretKey: key10
#   H2_PASSWORD:
#     secretName: credentials
#     secretKey: key13
#   TRUSTEES_CLIENT_SECRET:
#     secretName: credentials
#     secretKey: key16
#   ENC_SALT:
#     secretName: credentials
#     secretKey: key17

# =============================================================================
# VOLUMES & MOUNTS
# =============================================================================
volumeMounts:
  - name: report-service-secrets
    mountPath: /mnt/secrets-store/report-service
    readOnly: true
  - name: report-data
    mountPath: /mnt/report
  - name: secret-volume
    mountPath: /etc/secrets-store/report-service
    readOnly: true

volumes:
  - name: report-data
    emptyDir: {}
  - name: report-service-secrets
    csi:
      driver: secrets-store.csi.k8s.io
      readOnly: true
      volumeAttributes:
        secretProviderClass: report-service-secrets
  - name: secret-volume
    secret:
      secretName: credentials

# =============================================================================
# SERVICE
# =============================================================================
service:
  type: ClusterIP
  port: 8082
  targetPort: 20050
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "20050"
    prometheus.io/path: "/api/reports/actuator/prometheus"
    consul.hashicorp.com/service-name: "report-service"
    consul.hashicorp.com/service-tags: "springboot,backend,report"
    consul.hashicorp.com/service-meta-environment: "stag-env"
    consul.hashicorp.com/check-path: "/api/reports/actuator/health"
    consul.hashicorp.com/check-interval: "10s"
    consul.hashicorp.com/check-timeout: "5s"

# =============================================================================
# PROMETHEUS
# =============================================================================
prometheus:
  enabled: true
  path: /api/reports/actuator/prometheus
  port: 20050

# =============================================================================
# INGRESS — lives in backend-repo/shared-resources
# =============================================================================
ingress:
  enabled: false

# =============================================================================
# CERTIFICATE — prod only
# =============================================================================
certificate:
  enabled: false
