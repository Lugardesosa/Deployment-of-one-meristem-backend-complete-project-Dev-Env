
server:
  port: 8443
  ssl:
    key-store: classpath:keys/one-meristem-app.com-keystore.pkcs12
    key-store-password: ${KEY_STORE_PASSWORD}
    key-store-type: JKS
    key-alias: ${SSL_KEY_ALIAS}
    enabled: true

  server:
    http2:
      enabled: true