spring:
  datasource:
    url: jdbc:postgresql://${DB_SERVER}:${DB_PORT:5432}/${REPORT_DB_NAME}
    password: ${REPORT_DB_PASSWORD}
    username: ${REPORT_DB_USERNAME}
    driver-class-name: org.postgresql.Driver