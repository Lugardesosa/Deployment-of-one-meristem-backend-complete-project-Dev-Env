server:
 port: 20011
  debug=true

management:
  server:
    port: -1     # Disables separate actuator port (critical to avoid double-binding)
     Alternative: Set to a different port (e.g., 20012) if you need actuator

spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s  # Ensures clean container shutdown


