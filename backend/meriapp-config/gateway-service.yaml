spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: ${USER_SERVICE_URI}
          predicates:
            - Path=${one-app.users-service.context-path}/**
#          filters:
#            - PrefixPath=/api
