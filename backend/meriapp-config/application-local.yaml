spring:
  datasource:
    username: sa
    password: '{cipher}ccbb9e1abeb6dbf37b41169d0f8baf0504412cabf7948961db30b3e15bfb74d1'
    url: jdbc:h2:mem:mydb;DATABASE_TO_UPPER=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  flyway:
    locations: classpath:db/migration/h2

management:
  endpoints:
    web:
      exposure:
        include: '*'
        exclude: "env,beans,restart,shutdown"
        
  endpoint:
    health:
      show-details: always
      enabled: true

  health:
    mail:
      enabled: false
