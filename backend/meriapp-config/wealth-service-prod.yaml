spring:
  flyway:
    locations: classpath:db/migration/postgres
  datasource:
    url: ${DB_SERVER}/${WEALTH_DB_NAME}
    password: ${WEALTH_DB_PASSWORD}
    username: ${WEALTH_DB_USERNAME}
    driver-class-name: org.postgresql.Driver