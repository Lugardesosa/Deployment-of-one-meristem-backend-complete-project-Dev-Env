spring:
  datasource:
    url: jdbc:postgresql://${DB_SERVER}:${DB_PORT:5432}/${WEALTH_DB_NAME}
    password: ${WEALTH_DB_PASSWORD}
    username: ${WEALTH_DB_USERNAME}
    driver-class-name: org.postgresql.Driver