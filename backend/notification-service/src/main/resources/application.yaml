spring:
  application:
    name: notification-service

  config:
    import: configserver:${CONFIG_SERVER_URI}
  cloud:
    config:
      profile: local
      label: observabilityV2 # TODO: change to the branch being deployed
      username: ${ADMIN_USERNAME}
      password: ${ADMIN_PASSWORD}
      fail-fast: true
      retry:
        max-attempts: 10
        initial-interval: 3000
        max-interval: 3500


  termii:
    baseUrl: https://v3.api.termii.com
    apiKey: TLXeXGvxErNgYvvqZxOCQYvbIGCTTiJctbjWxhODZlCtlSauFQrJpzCeGjLRlh
    apiSecret: tsk_okU2kft3QCk7tJC2jMxi16oZUN
